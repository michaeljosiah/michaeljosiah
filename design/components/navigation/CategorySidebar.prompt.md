Projects-page taxonomy list: icon, tracked-caps label, right-aligned count. Active row gets a gold wash plus a 2px gold left edge — the only place a left accent border is allowed in this system.

```jsx
<CategorySidebar items={[{label:'All projects',icon:'layout-grid',count:14},{label:'Story worlds',icon:'feather',count:4}]} active="All projects" />
```