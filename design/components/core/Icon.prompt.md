Monochrome line icon that inherits the surrounding text colour — the brand's only icon treatment.

```jsx
<Icon name="arrow-right" size={16} />
```

Lucide is a **substitution** for the source render's icon set (no icon font or SVGs were supplied). Icons are never boxed, filled or multicolour; they always take the colour of their label group.