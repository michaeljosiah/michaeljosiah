All-caps hero headline, hard-broken into a stack of short period-terminated sentences.

```jsx
<DisplayHeading level={1} lines={['I build worlds.', "I build what's next."]} />
```

Always pass explicit lines — the line breaks are a design decision, not a wrap.