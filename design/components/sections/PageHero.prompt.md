Every route opens with this: right-anchored cinematic image, left scrim, eyebrow / caps headline / gold rule / blurb stack.

```jsx
<PageHero eyebrow="What I create" lines={['Ideas. Engineered.','Built to inspire.']}
  blurb="I build stories, platforms and systems…" image="assets/hero-portrait.png" />
```

Never place headline copy over bare imagery — the scrim is mandatory.