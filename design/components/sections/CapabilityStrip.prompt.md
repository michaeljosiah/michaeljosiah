Centred "BUILT WITH" row of icon + caps label pairs that closes the Home page.

```jsx
<CapabilityStrip items={[{icon:'brain',label:'AI'},{icon:'cloud',label:'Cloud'},{icon:'code',label:'Code'}]} />
```