Contact-form field. Placeholder carries the label; the trailing icon turns gold on focus.

```jsx
<TextField placeholder="Your Name" icon="user" />
<TextField placeholder="Your Message" icon="pencil" multiline rows={4} />
```