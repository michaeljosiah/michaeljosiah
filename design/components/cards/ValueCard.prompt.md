Centred icon + caps title + two hard-broken lines. Used in the Contact page's four-up values row, separated by hairlines.

```jsx
<ValueCard icon="heart" title="Purpose driven" lines={['Building with meaning','and impact.']} />
```