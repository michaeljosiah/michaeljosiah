Italic pull quote with a large gold open-quote glyph. One per page, used as section punctuation.

```jsx
<QuoteBlock image="assets/img-horizon-ring.png"
  quote="The best way to predict the future is to build it — together." attribution="Michael Josiah" />
```